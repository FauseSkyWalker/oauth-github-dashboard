# ===============================================
# OAuth 2.0 Demo - Environment Configuration
# ===============================================
# Copy this file to .env and fill in your values
# NEVER commit the actual .env file to version control!

# GitHub OAuth Application Settings
# ===============================================
# Create a new OAuth App at: https://github.com/settings/developers
# 1. Click "New OAuth App"
# 2. Application name: "My Analytics Dashboard" (or your choice)
# 3. Homepage URL: http://localhost:3000
# 4. Authorization callback URL: http://localhost:3000/callback
# 5. Copy the Client ID and Client Secret below

# Your GitHub OAuth App Client ID (public)
GITHUB_CLIENT_ID=your_github_client_id_here

# Your GitHub OAuth App Client Secret (keep this SECRET!)
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# OAuth Configuration
# ===============================================
# Redirect URI - MUST match EXACTLY what's configured in GitHub OAuth App
# Even trailing slashes matter! Default: http://localhost:3000/callback
REDIRECT_URI=http://localhost:3000/callback

# Session Configuration
# ===============================================
# Session Secret - Used to sign session cookies
# Generate a secure random string (32+ characters recommended)
# You can use: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-random-secret-string-at-least-32-chars

# Server Configuration
# ===============================================
# Server Port (optional, defaults to 3000)
# Change this if port 3000 is already in use
PORT=3000

# Node Environment
# Set to 'production' when deploying to production
# This enables secure cookies (HTTPS only) and other production optimizations
NODE_ENV=development

# Optional: Advanced Configuration
# ===============================================
# Session timeout in milliseconds (default: 3600000 = 1 hour)
# SESSION_TIMEOUT=3600000

# Enable debug logging (set to 'true' for verbose output)
# DEBUG=false

# Example values for testing (DO NOT USE IN PRODUCTION):
# ===============================================
# GITHUB_CLIENT_ID=Iv1.8a61f9b3a7aba766
# GITHUB_CLIENT_SECRET=generate_your_own_secret_never_use_this_example
# SESSION_SECRET=development-secret-change-this-in-production
# REDIRECT_URI=http://localhost:3000/callback

# Security Checklist:
# ===============================================
# [ ] Generated a strong, unique SESSION_SECRET
# [ ] Verified REDIRECT_URI matches GitHub OAuth app exactly
# [ ] Confirmed .env is in .gitignore
# [ ] Never committed real secrets to version control
# [ ] Using HTTPS in production (NODE_ENV=production)